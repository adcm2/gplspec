---
title: "Non-zero mapping benchmark"
description: "Benchmark for radial mapping"
layout: default
---

# Radial mapping benchmark

This benchmark compares the gravitational potential calculated via the pseudospectral method and the exact potential for a mapping given by $\xi(r) = (1+h)r$. The code that generates the data shown below is found in the tutorials.

<img src="{{ '/benchfigures/Bench4.png' | relative_url }}" alt="Benchmark 4 Results" class="figure-large">

*Figure: Comparison of gravitational potential calculations for a homogeneous spherical model with a non-zero mapping.*






